
|-----------------------------------------------------------------------------------------------------------------------

|-----------------------------------------------------------------------------------------------------------------------
    1. 자동 커밋 기능의 설정 상태를 확인 명령어.
       select @@autocommit;
    2. 자동 커밋 기능을 OFF로 설정하기.
        자동 커밋 기능을 off로 하려면, 다음과 같이 set autocommit=0; 이라는 명령을 실행합니다.
        set autocommit=0;

